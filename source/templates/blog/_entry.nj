{% extends '_layouts/_main.nj' %}

{{ config('page', {
  class : 'p-blog-post'
}) }}

{% block main %}

  <footer class='h-opacity--30 h-bleed-top h-margin-bottom+ h-align--center'>
    {{ moment(page.date).format('lll') }} — {{ page.author|render }}
  </footer>

  <div class='s-blog-post Wrapper--readable'>
    {% block post %}{% endblock %}
  </div>

{% endblock %}